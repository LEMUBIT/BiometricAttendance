<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lemuel.lemubit.com.biometricattendance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.rollbar.android.ACCESS_TOKEN"
            android:value="a81fdaa661dd4864b1e35b0a48bc57e7" />

        <activity android:name=".view.activity.AdminHomeActivity" />
        <activity android:name=".view.activity.NewCaptureActivity" />
        <activity android:name=".view.activity.ViewUsersActivity" />
        <activity android:name=".view.activity.ViewAttendanceActivity"></activity>
    </application>

</manifest>